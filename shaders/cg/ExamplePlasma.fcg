//===----------------------------------------------------------------------===//
//
// Part of the gcmgl, under the MIT License.
// See LICENSE for license information.
// SPDX-License-Identifier: MIT
//
//===----------------------------------------------------------------------===//

struct FragmentInput_t
{
    float3 objPos : TEXCOORD0;
};

struct FragmentOutput_t
{
    float4 color : COLOR;
};

FragmentOutput_t main(FragmentInput_t input, uniform float Time)
{
    FragmentOutput_t output;

    float x = input.objPos.x;
    float y = input.objPos.y;
    float z = input.objPos.z;

    float v = sin(x * 6.0 + Time * 1.5)
            + sin(y * 6.0 + Time * 1.2)
            + sin((x + y + z) * 4.0 + Time)
            + sin(sqrt(x * x + y * y + z * z + 1.0) * 5.0 - Time * 2.0);

    v = v * 0.25 + 0.5;

    float r = sin(v * 3.14159);
    float g = sin(v * 3.14159 + 2.094);
    float b = sin(v * 3.14159 + 4.189);

    output.color = float4(r, g, b, 1.0);

    return output;
}